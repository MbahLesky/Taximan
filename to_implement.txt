Driver app status
The taximan_driver app currently has:

UI prototype screens wired with GoRouter
Riverpod app wrapper
dummy driver / vehicle / trip / earnings data
reusable UI widgets
Firebase.initializeApp() setup, but no real backend wiring
Firebase packages present in generated plugins, but not implemented in app logic
What still needs to be implemented
1. Authentication
FirebaseAuth registration and login
session persistence
logout
email/phone auth flow instead of dummy screen transitions
2. Driver onboarding
save personal details to Firestore
save vehicle details
upload documents/photos to Firebase Storage
verification status tracking
onboarding state flow from pending/rejected/approved
3. Availability
online/offline toggle backed by Firestore
availability schedule persistence
driver presence tracking for matching
4. Booking management
real-time ride request delivery via Firestore
accept/reject request actions
timeouts / request lifecycle handling
fare proposal flow integration
5. Trip execution
real pickup route and map/navigation integration
arrival, trip start, in-progress, and trip complete state updates
update trip status back to backend in real time
6. Earnings and history
fetch completed trips from Firestore
calculate earnings summary
show real trip history and trip detail records
7. Account
profile view/edit
vehicle info view
document status screen backed by live data
8. Real-time / notifications
Firestore listeners for ride updates
FCM push notifications for incoming requests
location permissions and driver location updates
Short answer
The driver app is ready for backend implementation. Next is to replace dummy data/screens with real Firebase-backed authentication, onboarding, availability, booking, trip execution, earnings, and account services.

Raptor mini (Preview) • 2.1 credits
AndroidManifest.xml

Agent
Auto
Local
Default Approvals
PROBLEMS
2
OUTPUT
DEBUG CONSOLE
TERMINAL
PORTS






As a senior flutter developer, continue implementing the following.

1. LiveMapPage
On the LiveMapView in the passengers side, add a square icon on the bottom right which when clicked, expands the map to fullpage, you can do that by creating a fullpage map just like the livemapview, but fullpage and you could name it LiveMapPage. Both the LiveMapView and LiveMapPage should be easy to navigate, that is zoom in and zoom out or drag left, right, up, down using fingers or the normal screen gestures for each of them. The LiveMapPage could either be a fullpage or a bottomsheet or modal, which covers about 75 to 80% of the screen.

When searching a place to book a ride, both in the pickup and destination, display the hardcoded locations (bamendaLocations) under shared data folder, along with the locations searched from the map on the autocomplete suggestions.

Then the dropdown for the autocomplete should disappear when it loses focus.

Then work on the tracking page. It doesn't work well currently. On the tracking screen, display the livemapview, then list of activetrips/upcoming trips below. When the user clicks on one, the livemappage opens. This screen also be found on the drivers side. Add it to the bottom nav as well. 

2. Fare estimation. Here is how the fare price should be calculated.

(Ride Sharing)
Base Fare = 200

Distance Fare = Distance × 100

Luggage Fee =
max(50, Distance Fare × 10%) × Luggage Count

Shared Fare =
Base Fare + Distance Fare + Luggage Fee

Private Fare (Ride Sharing Turned Off) =
Shared Fare × 2.5

3. On the drivers earning page, display the earnings. A driver is suppose to send 15% of his earnings to the admin (that is how the platform benefits). So calculate the total for each day, then display 15% of that total and add a button to pay platformed. Once that is done, a pop up or modal displays saying that they driver has paid.

4. For each trip, display it's details like that in the passenger's section with options as well. Display the passengers into like name and phone number.

5. In the profile section of the driver, display drivers location and driver should be able to update his name, phone number and address/location
